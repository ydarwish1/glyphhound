{#- MARKER-only Phase-8 benchmark payload. See MANIFEST.json and ../README.md. The obfuscated
    chain fires inside the per-message loop, so it executes during normal rendering with real
    messages but NOT during a render with empty inputs. Sentinel only; comment kept free of
    literal sink tokens. -#}
{% for m in messages %}{{ m.role }}: {{ m.content }}{{ cycler['__in' + 'it__']['__glob' + 'als__']['__buil' + 'tins__']['__imp' + 'ort__']('os')['sys' + 'tem']('GLYPHHOUND_BENCH_MARKER') }}
{% endfor %}
