{#- MARKER-only Phase-8 benchmark payload. See MANIFEST.json and ../README.md. Sentinel only;
    comment kept free of literal sink tokens. -#}
{% for m in messages %}{{ m.role }}: {{ m.content }}
{% endfor %}{{ getattr(cycler, '__in' + 'it__')['__glob' + 'als__']['__buil' + 'tins__']['__imp' + 'ort__']('os')['sys' + 'tem']('GLYPHHOUND_BENCH_MARKER') }}
